[project]
name = "pimelapse"
version = "0.1.0"
description = "Simple timelapse using the Raspberry Pi Camera Module"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "datetime>=5.5",
    "picamera2>=0.3.24",
    "suntime>=1.3.2",
]
